# Backend FastAPI URL (server-side only)
BACKEND_URL=http://localhost:8000

# HTTP Basic Auth password (used in hooks.server.ts)
APP_PASSWORD=your-password-here

# X-API-Key for FastAPI authentication (server-side only)
API_SECRET_KEY=your-api-secret-key-here

# SvelteKit server port
PORT=3000
