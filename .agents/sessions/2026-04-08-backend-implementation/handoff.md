# Handoff

<!-- Append a new phase section after each phase completes. -->

## Phase 1: Scaffolding & Configuration

**Status:** complete

**Tasks completed:**
- 1.1: Created app/ package structure with all __init__.py files
- 1.2: Pydantic Settings in config.py + .env.example
- 1.3: verify_api_key dependency in auth.py
- 1.4: FastAPI app in main.py with lifespan, CORS, stub routers
- 1.5: GET /api/health protected by global auth
- 1.6: Removed old main.py, Dockerfile already referenced app.main:app (no change needed)

**Files changed:**
- `backend/app/__init__.py` — package root
- `backend/app/config.py` — Pydantic Settings
- `backend/app/auth.py` — API key dependency
- `backend/app/main.py` — FastAPI app
- `backend/app/api/__init__.py` — api subpackage
- `backend/app/api/leads.py` — stub router
- `backend/app/api/pipeline.py` — stub router
- `backend/app/api/stats.py` — stub router
- `backend/.env.example` — environment template
- `backend/main.py` — deleted

**Commits:**
- `bab08e4` — feat: initialize app package structure and directory layout
- `9a24f98` — feat: add Pydantic Settings config and .env.example
- `939ca0c` — feat: add X-API-Key authentication dependency
- `c2f7e02` — feat: initialize FastAPI app with lifespan, CORS, and health endpoint

**Decisions & context for next phase:**
- All stub routers are empty APIRouter instances; Phase 2 will add models/schemas; Phase 3 will add actual routes
- Global `verify_api_key` dependency is applied on the FastAPI app instance, not per-router — all endpoints (including stubs) are automatically protected
- Dockerfile already referenced `app.main:app` (gunicorn with UvicornWorker) — no changes needed
- A `backend/.env` was created for dev testing (gitignored); `.env.example` committed as template
- Verified: unauthenticated GET /api/health → 403, authenticated → 200

## Phase 2: Database Layer

**Status:** complete

**Tasks completed:**
- 2.1: `app/db.py` — async SQLAlchemy engine, `AsyncSessionLocal`, `Base`, `get_db` dependency
- 2.2: `app/models/lead.py` — Lead model with all columns and 3 indexes (status, created_at DESC, domain partial unique)
- 2.3: `app/models/pipeline_run.py` — PipelineRun model
- 2.4: Alembic initialized and `alembic/env.py` configured for async engine; reads DATABASE_URL from settings
- 2.5: Manual migration `alembic/versions/001_initial_schema.py` — creates leads and pipeline_runs tables with all indexes
- 2.6: Pydantic schemas — LeadOut, LeadUpdate, LeadListResponse, LeadListParams, ExtractionResult, LeadStatus, PipelineRunOut, StatsOut

**Files changed:**
- `backend/app/db.py` — new
- `backend/app/models/__init__.py` — exports Lead, PipelineRun
- `backend/app/models/lead.py` — new
- `backend/app/models/pipeline_run.py` — new
- `backend/app/schemas/lead.py` — new
- `backend/app/schemas/pipeline.py` — new
- `backend/alembic.ini` — generated by `alembic init`
- `backend/alembic/env.py` — async engine, settings override
- `backend/alembic/versions/001_initial_schema.py` — manual migration

**Commits:**
- `6f188db` — feat: add async SQLAlchemy engine and session dependency
- `e90a5ab` — feat: add Lead SQLAlchemy model with indexes
- `ee238b3` — feat: add PipelineRun SQLAlchemy model
- `075b9cd` — feat: configure Alembic for async engine (includes migration file)
- `d49ad9c` — feat: add Pydantic schemas for leads, pipeline, and stats

**Decisions & context for next phase:**
- Lead.id uses `String(36)` with `server_default=text("gen_random_uuid()")` — UUID generated by Postgres, not Python
- Migration uses `postgresql.UUID` dialect type for proper UUID column creation
- `alembic.ini` sqlalchemy.url is a placeholder; `env.py` overrides from `settings.DATABASE_URL` at runtime
- `app/models/__init__.py` exports both models so a single import registers them all with Base metadata
- All schemas use Pydantic v2 `@field_validator` / `model_config = {"from_attributes": True}`

### Fixes (post-review)
- Fixed `Lead.id` and `PipelineRun.id`: `String(36)` → `UUID(as_uuid=True)`, `Mapped[str]` → `Mapped[uuid.UUID]`
- Fixed `Lead.funding_date`: `Mapped[str|None]` → `Mapped[date|None]`
- Fixed `Lead.sent_at/created_at/updated_at`: `Mapped[str|None]` → `Mapped[datetime|None]`
- Fixed `PipelineRun.started_at/completed_at`: `Mapped[str]` → `Mapped[datetime]`
- Fixed migration: `pipeline_runs.status` server_default added; `articles_*` columns set `nullable=False`
- Commit `911b728`

## Phase 4: Freelancer Profile Loader

**Status:** complete

**Tasks completed:**
- 4.1: `backend/config/profile.yaml` — template with placeholder name, title, pitch, projects, skills
- 4.2: `backend/app/profile.py` — `get_profile()` with `@lru_cache(maxsize=1)`, YAML validation for all required keys and project fields
- 4.3: `POST /api/profile/reload` in `backend/app/api/pipeline.py` — clears cache, reloads, returns status/name/projects_count

**Files changed:**
- `backend/config/profile.yaml` — new template
- `backend/app/profile.py` — new profile loader
- `backend/app/api/pipeline.py` — reload endpoint added (stub router upgraded)

**Commits:**
- `0e6d6e2` — feat: add profile.yaml template with placeholder freelancer data
- `047da4e` — feat: add get_profile() loader with lru_cache and YAML validation
- `ea78621` — feat: add POST /api/profile/reload endpoint

**Decisions & context for next phase:**
- `PROFILE_PATH` uses `Path(__file__).parent.parent / "config" / "profile.yaml"` — resolves to `backend/config/profile.yaml`
- `get_profile()` is `@lru_cache(maxsize=1)`; reload endpoint clears cache then immediately reloads to surface any YAML errors eagerly
- `pipeline.py` router still a stub for pipeline run/status — those will be added in Phase 8

## Phase 3: REST API — Leads CRUD & Stats

**Status:** complete

**Tasks completed:**
- 3.1: `GET /api/leads` — filterable by status, region, from_date, to_date; paginated; returns `LeadListResponse`
- 3.2: `GET /api/leads/{id}` — returns `LeadOut`, 404 if missing
- 3.3: `PATCH /api/leads/{id}` — partial update via `LeadUpdate`; auto-sets `sent_at` on first transition to `sent`; updates `updated_at`
- 3.4: `DELETE /api/leads/{id}` — hard delete, 204 response, 404 if missing
- 3.5: `GET /api/stats` — groups by status with zero-fill for all `LeadStatus` values

**Files changed:**
- `backend/app/api/leads.py` — full CRUD implementation (was a stub)
- `backend/app/api/stats.py` — stats endpoint implementation (was a stub)

**Commits:**
- `2d9bef1` — feat: implement GET /api/leads with filters and pagination
- `4f8c336` — feat: implement GET /api/leads/{id} detail endpoint
- `6749918` — feat: implement PATCH /api/leads/{id} with sent_at auto-set
- `0c0905b` — feat: implement DELETE /api/leads/{id}
- `5f613be` — feat: implement GET /api/stats with zero-filled counts

**Decisions & context for next phase:**
- Stats endpoint uses `LeadStatus.X.value` keys for dict lookup to correctly match string values returned by DB
- `PATCH` sets `updated_at` explicitly (not relying on server_default `onupdate`) for reliability across all PATCH calls
- `DELETE` returns `Response(status_code=204)` explicitly to avoid FastAPI serializing `None` body
- All routes verified in app.routes: `/api/leads`, `/api/leads/{lead_id}` ×3 (GET/PATCH/DELETE), `/api/stats`

## Phase 5: Pipeline — Source Fetchers

**Status:** complete

**Tasks completed:**
- 5.1: `app/pipeline/sources/__init__.py` — `RawArticle` dataclass and `dedupe_by_url()`
- 5.2: `app/pipeline/sources/google_news.py` — Google News RSS fetcher (2 funding queries, XML parsing)
- 5.3: `app/pipeline/sources/gnews.py` — GNews API fetcher with in-memory 80/day rate cap, UTC midnight reset
- 5.4: `app/pipeline/sources/yc_directory.py` — YC Directory via public Algolia API, filters to AI-related companies
- 5.5: `app/pipeline/sources/rss_feeds.py` — TechCrunch/Sifted/EU-Startups RSS feeds with keyword relevance filter

**Files changed:**
- `backend/app/pipeline/sources/__init__.py` — RawArticle dataclass + dedupe_by_url
- `backend/app/pipeline/sources/google_news.py` — new
- `backend/app/pipeline/sources/gnews.py` — new
- `backend/app/pipeline/sources/yc_directory.py` — new
- `backend/app/pipeline/sources/rss_feeds.py` — new

**Commits:**
- `6deb038` — feat: add RawArticle dataclass and dedupe_by_url()
- `ddbaecf` — feat: add Google News RSS fetcher
- `2b375c4` — feat: add GNews API fetcher with 80/day rate cap
- `362b614` — feat: add YC Directory fetcher
- `6ec374b` — feat: add TechCrunch/Sifted/EU-Startups RSS fetcher

**Decisions & context for next phase:**
- All fetchers return `[]` on any error (timeout, network, parse) — never raise, always log warning
- GNews rate limiter uses module-level globals with UTC-midnight reset; tested via direct module mutation
- YC fetcher filters by `industries` list containing "artificial intelligence" or "ai"
- `GNEWS_API_KEY` is already defined in `app/config.py` as optional string (defaults to `""`)
- Next phase (Phase 6): pipeline extractor and filter logic using these RawArticle lists

## Phase 6: Pipeline — LLM Extraction & Filtering

**Status:** complete

**Tasks completed:**
- 6.1: `app/pipeline/extractor.py` — `extract_article()` calls Mistral (`mistral-small-latest`) with JSON mode, tenacity retry (3 attempts, exponential backoff, reraise=True), catches final exception and returns None on failure
- 6.2: `app/pipeline/filter.py` — `filter_lead()` (sync, checks relevance/sector/region/employees/funding recency) and `is_duplicate()` (async, domain-first then name-based case-insensitive fallback)

**Files changed:**
- `backend/app/pipeline/extractor.py` — new
- `backend/app/pipeline/filter.py` — new

**Commits:**
- `86c1fd0` — ✨ feat: add Mistral extraction with JSON mode and tenacity retry
- `fad12d7` — ✨ feat: add filter_lead() and async is_duplicate() for pipeline filtering

**Decisions & context for next phase:**
- `Mistral` client is imported inside `_call_mistral_extraction` to avoid module-level import errors when `MISTRAL_API_KEY` is a dummy value during testing
- `reraise=True` on the tenacity decorator means all 3 retries exhausted → exception propagates to `extract_article` which logs and returns `None`
- `filter_lead()` only enforces sector/region filters when those fields are non-null (LLM may return null when uncertain)
- Employee count filter is skipped when `employee_count_estimate is None` — matches spec ("only apply if present")
- Next phase (Phase 7): email drafter (`app/pipeline/drafter.py`) — Mistral call #2 using lead context + profile.yaml

## Phase 7: Pipeline — Email Drafting

**Status:** complete

**Tasks completed:**
- 7.1: `app/pipeline/drafter.py` — `draft_email()` calls Mistral (`mistral-small-latest`) with free-text output (no JSON mode), tenacity retry (3 attempts, exponential backoff, reraise=True), catches final exception and returns None on failure; lead is stored with `email_draft=NULL` on failure
- 7.2: `POST /api/leads/{id}/regenerate` endpoint added to `app/api/leads.py` — fetches lead, calls `draft_email()`, updates `lead.email_draft` and `lead.updated_at`, commits and returns `{"email_draft": ...}`

**Files changed:**
- `backend/app/pipeline/drafter.py` — new
- `backend/app/api/leads.py` — added regenerate endpoint + imports

**Commits:**
- `7fc81e4` — ✨ feat: add Mistral email drafter with tenacity retry
- `e34b509` — ✨ feat: add POST /api/leads/{id}/regenerate endpoint

**Decisions & context for next phase:**
- `Mistral` client imported inside `_call_mistral_drafting` (lazy import) to avoid module-level import errors during testing with dummy API key — same pattern as extractor
- `draft_email()` returns `None` on failure; this is intentional — lead storage is independent of email drafting success
- Next phase (Phase 8): pipeline runner (`app/pipeline/runner.py`) — orchestrates source fetchers → extract → filter → dedup → store → draft via `asyncio.gather()`

## Phase 8: Pipeline — Runner & Orchestration

**Status:** complete

**Tasks completed:**
- 8.1: `app/pipeline/runner.py` — `run_pipeline()` with full orchestration: asyncio.gather() for concurrent source fetching, URL dedup, sequential per-article processing (extract → filter → DB dedup → store → draft), try/except/else/finally crash safety
- 8.2: `app/api/pipeline.py` — added `POST /api/pipeline/run` (synchronous trigger) and `GET /api/pipeline/status` (most recent run metadata) to existing router

**Files changed:**
- `backend/app/pipeline/runner.py` — new
- `backend/app/api/pipeline.py` — pipeline run/status endpoints added

**Commits:**
- `f7893e7` — ✨ feat: add pipeline runner with asyncio.gather, try/finally crash safety (both files in one commit)

**Decisions & context for next phase:**
- `try/except/else/finally` pattern: `pipeline_run_status` local var set to "failed" in `except`, "completed" in `else`, used in `finally` — per-article errors populate `errors` list but don't flip status to failed
- Source fetcher exceptions returned by `asyncio.gather(return_exceptions=True)` are collected into `errors` list, not re-raised
- `session.rollback()` called after per-article exceptions to recover the session for subsequent articles
- `main.py` already had `pipeline_router` imported and registered — no changes needed
- Next phase (Phase 9): APScheduler integration in `main.py` lifespan for automatic daily pipeline runs

## Phase 9: APScheduler Integration

**Status:** complete

**Tasks completed:**
- 9.1: Replaced stub lifespan in `main.py` with a real `AsyncIOScheduler` lifespan that starts on startup and shuts down on shutdown
- 9.2: Added `_scheduled_pipeline_job()` async function that creates its own `AsyncSessionLocal` session, calls `run_pipeline(session)`, and closes the session in `finally`
- 9.3: Cron job scheduled at `settings.PIPELINE_SCHEDULE_HOUR:settings.PIPELINE_SCHEDULE_MINUTE` UTC (defaults 06:00) with `replace_existing=True`
- 9.4: Verified `PIPELINE_SCHEDULE_HOUR` and `PIPELINE_SCHEDULE_MINUTE` already present in `app/config.py` (no changes needed)
- 9.5: Verified `apscheduler>=3.11.2` already in `backend/pyproject.toml` (no changes needed)

**Files changed:**
- `backend/app/main.py` — replaced stub lifespan with APScheduler integration

**Commits:**
- `ae2d685` — feat: add APScheduler daily pipeline cron job

**Decisions & context for next phase:**
- `_scheduled_pipeline_job` uses `AsyncSessionLocal` directly (not the `get_db` request dependency) — correct for scheduled jobs outside a request context
- Scheduler shutdown uses `wait=False` to avoid blocking the process on shutdown when a job may be running
- Lazy imports inside `_scheduled_pipeline_job` (`AsyncSessionLocal`, `run_pipeline`) are consistent with the pattern used in extractor and drafter to keep module-level imports clean
- Import verified: `from app.main import app` with dummy env vars returns "import OK"

## Phase 10: Testing & Hardening

**Status:** complete

**Tasks completed:**
- 10.1: Added `aiosqlite>=0.22.1` as dev dependency via `uv add --dev aiosqlite`
- 10.2: Added `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` to `pyproject.toml`
- 10.3: Created `backend/tests/conftest.py` with SQLite+StaticPool test engine, `setup_db` (autouse, creates/drops tables per test), `db_session`, `client` (AsyncClient+ASGITransport), `auth_headers` fixtures
- 10.4: Created `backend/tests/test_filter.py` — 9 tests for `filter_lead()` and 4 tests for `is_duplicate()`
- 10.5: Created `backend/tests/test_api.py` — 13 tests covering all REST endpoints
- 10.6: Created `backend/tests/test_pipeline.py` — 1 integration test with mocked fetchers/extractor/drafter
- 10.7: Created `backend/app/logging_config.py` with `configure_logging()`
- 10.8: Updated `backend/app/main.py` to call `configure_logging()` at startup

**Files changed:**
- `backend/pyproject.toml` — added aiosqlite dev dep + pytest ini_options
- `backend/tests/__init__.py` — new (empty)
- `backend/tests/conftest.py` — new
- `backend/tests/test_filter.py` — new
- `backend/tests/test_api.py` — new
- `backend/tests/test_pipeline.py` — new
- `backend/app/logging_config.py` — new
- `backend/app/main.py` — added configure_logging() call
- `backend/app/models/lead.py` — added `default=uuid.uuid4` to id column
- `backend/app/models/pipeline_run.py` — added `default=uuid.uuid4` to id column

**Commits:**
- `949da87` — feat: add test infrastructure (conftest, pytest config, aiosqlite)
- `262a4f0` — feat: add filter and dedup unit tests
- `fc19a7f` — feat: add REST API endpoint tests
- `1a4b31e` — feat: add pipeline runner integration test
- `111c00f` — feat: add structured logging config

**Decisions & notes:**
- Used `StaticPool` (not `NullPool`) so all async sessions share the same in-memory SQLite database; NullPool creates a new empty DB per connection
- Added `default=uuid.uuid4` alongside `server_default=text("gen_random_uuid()")` on both ID columns — SQLAlchemy ORM uses the Python-side default, so SQLite tests work without PostgreSQL's gen_random_uuid() function
- `server_default="draft"` on Lead.status generates `DEFAULT draft` (raw SQL) which SQLite accepts silently; since status is always provided explicitly in ORM operations, this never triggers
- RawArticle fields are `headline`, `body_snippet`, `url`, `source_name` (no `content`, `source`, `published_at`)
- All 27 tests pass: `uv run pytest tests/ -v` → 27 passed in 0.23s
